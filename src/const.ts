export enum TerritoryType {
    LAND,
    COAST,
    SEA
}

export enum OrderType {
    MOVE,
    SUPPORT_MOVE,
    HOLD,
    SUPPORT_HOLD,
    CONVOY,
    BUILD,
    DESTROY
}

export enum UnitType {
    ARMY,
    FLEET
}